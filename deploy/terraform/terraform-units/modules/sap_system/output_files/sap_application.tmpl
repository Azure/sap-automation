**SAP System Deployment**

The SAP Application (${sid}) has been deployed to resource group **${resource_group_name}** in subscription **${subscription_id}**.

You can access the resource group directly in the Azure Portal using the following link: [${resource_group_name}](${url})



The following servers have been deployed:


| Role                           | Names                           |
| :----------------------------- | :------------------------------ |
| Database Servers               | ${db_servers}                   |
| SCS Servers:                   | ${scs_servers}                  |
| PAS Server:                    | ${pas_server}                   |
| Application Servers:           | ${application_servers}          |
| Web Dispatcher Servers:        | ${webdisp_servers}              |


**Access credentials**

The username is stored in the key vault: **${kv_name}** in a secret named **${username_secret_name}**
The SSH key is stored in the key vault: **${kv_name}** in a secret named **${ssh_secret_name}**
The password key stored in the key vault: **${kv_name}** in a secret named **${password_secret_name}**


**SAP Application Configuration**


| Setting                  | Value                           |
| :----------------------- | :------------------------------ |
| SID                      | ${sid}                          |
| Database                 | ${platform}                     |
| DB SID                   | ${db_sid}                       |
| SCS Server IP            | ${scs_server_loadbalancer_ip}   |
| SCS High Availability    | ${scs_high_availability}        |
| DB High Availability     | ${database_high_availability}   |

