# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

# /*----------------------------------------------------------------------------8
# |                                                                            |
# |            Role for ensuring the swap space is configured correctly         |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
---
# -------------------------------------+---------------------------------------8
#
# Task: 1.1     - swap space setup
#
# -------------------------------------+---------------------------------------8


- name:                                "1.1 Swap - Ensure /mnt/resource directory exists"
  ansible.builtin.file:
    path:                              "/mnt/resource"
    state:                             directory
    mode:                              "0755"
  notify:
    - "1.1 Swap - Restart waagent"

# - name:                                "1.1 Swap - Update WAAgent on {{ ansible_os_family }}"
#   ansible.builtin.dnf:
#     name:                              WALinuxAgent
#     state:                             latest

#   when:
#     - ansible_os_family == 'RedHat'
#   tags:
#     - skip_ansible_lint

# - name:                                "1.1 Swap - Update WAAgent on {{ ansible_os_family }}"
#   community.general.zypper:
#     name:                              WALinuxAgent
#     state:                             latest
#   environment:
#     ZYPP_LOCK_TIMEOUT:                 "60"
#   when:
#     - distribution_id == 'sles_sap15'
#   tags:
#     - skip_ansible_lint

- name:                                "1.1 Swap - Restart WAAgent on {{ distribution_id }}"
  ansible.builtin.service:
    name:                              waagent
    state:                             restarted

# Check the current swap size
- name:                                "1.1 Swap - Check swap size"
  ansible.builtin.shell:               set -o pipefail && cat /proc/meminfo | grep SwapTotal
  register:                            swap_space

- name:                                "1.1 Swap - Show SWAP space from /proc/meminfo"
  ansible.builtin.debug:
    msg:                               "SWAP size from procinfo: {{ swap_space }}"
    verbosity:                         2

- name:                                "1.1 Swap - Store SWAP space value"
  ansible.builtin.set_fact:
    swap_value:                        "{{ swap_space.stdout_lines[0] | regex_search('([0-9]+)') }}"

- name:                                "1.1 Swap - Convert Swap space to Integer"
  ansible.builtin.set_fact:
    swap_size:                         "{{ swap_value | int }}"

- name:                                "1.1 Swap - Show SWAP size"
  ansible.builtin.debug:
    msg:                               "Swap size: {{ swap_size }}"
    verbosity:                         2

- name:                                "1.1 Swap - Detect NVMe device count for swap helper"
  ansible.builtin.shell:               set -o pipefail && lsblk -dn -o NAME,TYPE | awk '$2 == "disk" && $1 ~ /^nvme/ {count++} END {print count + 0}'
  args:
    executable:                        /bin/bash
  register:                            nvme_device_probe
  changed_when:                        false
  failed_when:                         false

- name:                                "1.1 Swap - Store NVMe device count"
  ansible.builtin.set_fact:
    nvme_device_count:                 "{{ nvme_device_probe.stdout | trim | int }}"

- name:                                "1.1 Swap - Detect Microsoft NVMe Direct disk count from sysfs/udev"
  ansible.builtin.shell: |
                                       set -o pipefail
                                       direct_count=0
                                       while read -r device_name; do
                                         [ -n "$device_name" ] || continue
                                         model_file="/sys/class/block/${device_name}/device/model"
                                         if [ -e "$model_file" ] && grep -Eq "MSFT NVMe Accelerator|Microsoft NVMe Direct Disk" "$model_file"; then
                                           direct_count=$((direct_count + 1))
                                           continue
                                         fi

                                         if command -v udevadm >/dev/null 2>&1 && \
                                            udevadm info --query=property --name="/dev/${device_name}" 2>/dev/null | \
                                            grep -Eq "^ID_MODEL=(MSFT NVMe Accelerator|Microsoft NVMe Direct Disk)"; then
                                           direct_count=$((direct_count + 1))
                                         fi
                                       done < <(lsblk -dn -o NAME,TYPE | awk '$2 == "disk" && $1 ~ /^nvme/ {print $1}')
                                       echo "$direct_count"
  args:
    executable:                        /bin/bash
  register:                            nvme_direct_probe
  changed_when:                        false
  failed_when:                         false
  when:
    - nvme_device_count | int > 0

- name:                                "1.1 Swap - Store Microsoft NVMe Direct disk count"
  ansible.builtin.set_fact:
    nvme_direct_count:                 "{{ (nvme_direct_probe.stdout | default('0') | trim | int) if (nvme_device_count | int > 0) else 0 }}"

- name:                                "1.1 Swap - Show NVMe detection summary"
  ansible.builtin.debug:
    msg:
      - "use_nvme_disks: {{ use_nvme_disks | default(false) }}"
      - "swap_size: {{ swap_size | int }}"
      - "nvme_device_count: {{ nvme_device_count | int }}"
      - "nvme_direct_count: {{ nvme_direct_count | int }}"
    verbosity:                         2

- name:                                "1.1 Swap - Skip NVMe helper when no NVMe devices are detected"
  ansible.builtin.debug:
    msg:                               "Skipping azure-nvme-swap-conf: no NVMe devices detected (mixed NVMe/SCSI presentation possible)."
    verbosity:                         1
  when:
    - use_nvme_disks | default(false) | bool
    - swap_size | int == 0
    - nvme_device_count | int == 0

- name:                                "1.1 Swap - Skip NVMe helper when no Microsoft NVMe Direct disks are detected"
  ansible.builtin.debug:
    msg:                               "Skipping azure-nvme-swap-conf: NVMe devices exist but no Microsoft NVMe Direct disks were detected."
    verbosity:                         1
  when:
    - use_nvme_disks | default(false) | bool
    - swap_size | int == 0
    - nvme_device_count | int > 0
    - nvme_direct_count | int == 0

- name:                                "1.1 Swap - Configure NVMe VMs for Swap"
  when:
    - use_nvme_disks | default(false) | bool
    - swap_size | int == 0
    - nvme_device_count | int > 0
    - nvme_direct_count | int > 0
  block:
    # Install required dependencies for the NVMe swap helper script
    - name:                            "1.1 Swap - Install NVMe swap dependencies (RHEL)"
      when:                            ansible_os_family | upper == "REDHAT"
      ansible.builtin.dnf:
        name:
          - nvme-cli
          - mdadm
          - gdisk
        state:                         present
        disablerepo:                   "epel*"
        skip_broken:                   true
        nobest:                        true

    - name:                            "1.1 Swap - Install NVMe swap dependencies (SUSE)"
      when:                            ansible_os_family | upper == "SUSE"
      ansible.builtin.package:
        name:
          - nvme-cli
          - mdadm
          - gdisk
        state:                         present
      environment:
        ZYPP_LOCK_TIMEOUT:             "20"

    # copy nvme disk config script for temporary disks
    - name:                            "1.1 Swap - Create Azure NVMe temp disk configuration helper script"
      ansible.builtin.copy:
        src:                           88-azure-nvme-swap-conf
        dest:                          /usr/local/bin/azure-nvme-swap-conf
        mode:                          '0755'
        owner:                         root
        group:                         root
        force:                         true
      register:                        helper_script_created

    # touch /etc/mdadm/mdadm.conf to avoid errors in the script
    - name:                            "1.1 Swap - Ensure /etc/mdadm/mdadm.conf exists"
      ansible.builtin.file:
        path:                          /etc/mdadm/mdadm.conf
        mode:                          '0644'
        state:                         touch
      when:                            helper_script_created is changed

    # run the nvme disk config script
    - name:                            "1.1 Swap - Run Azure NVMe temp disk configuration helper script"
      ansible.builtin.command:         /usr/local/bin/azure-nvme-swap-conf ext4 /mnt/resource
      register:                        nvme_swap_configured
      changed_when:                    nvme_swap_configured.rc == 0 and "'Skipping' not in nvme_swap_configured.stdout"
      failed_when:                     nvme_swap_configured.rc != 0 and 'No NVMe Direct Disks found' not in ((nvme_swap_configured.stdout | default('')) ~ ' ' ~ (nvme_swap_configured.stderr | default(''))) and 'Found 0 NVMe Direct Disks' not in ((nvme_swap_configured.stdout | default('')) ~ ' ' ~ (nvme_swap_configured.stderr | default('')))

    - name:                            "1.1 Swap - Show Azure NVMe temp disk configuration helper script output"
      ansible.builtin.debug:
        msg:
          - "rc: {{ nvme_swap_configured.rc }}"
          - "stdout: {{ nvme_swap_configured.stdout | default('') }}"
          - "stderr: {{ nvme_swap_configured.stderr | default('') }}"
        verbosity:                     2

- name:                                "1.1 Swap - Check for (waagent_conf)"
  ansible.builtin.stat:
    path:                              /etc/waagent.conf
  register:                            waagent_conf

- name:                                "1.1 Swap - Check for (waagent.conf.rpmsave)"
  become:                              true
  become_user:                         root
  ansible.builtin.stat:
    path:                              /etc/waagent.conf.rpmsave
  register:                            waagent_conf_save

- name:                                "1.1 Swap: Copy the conf file"
  become:                              true
  become_user:                         root
  ansible.builtin.copy:
    remote_src:                        true
    src:                               /etc/waagent.conf.rpmsave
    dest:                              /etc/waagent.conf
    mode:                              preserve
  when:
    - not waagent_conf.stat.exists
    - waagent_conf_save.stat.exists
  register:                            waagent_conf_copy


- name:                                "1.1 Swap - Force systemd to reread configuration {{ distribution_id }}"
  ansible.builtin.service:
    name:                              systemd
    state:                             reloaded
  when:
    - waagent_conf_copy is defined
    - waagent_conf_copy.changed

- name:                                "1.1 Swap - Restart WAAgent on {{ distribution_id }}"
  ansible.builtin.service:
    name:                              waagent
    state:                             restarted
  when:
    - waagent_conf_copy is defined
    - waagent_conf_copy.changed

- name:                                "1.1 Swap - Ensure waagent file is configured with correct parameters"
  ansible.builtin.lineinfile:
    dest:                              /etc/waagent.conf
    state:                             "{{ item.state }}"
    regexp:                            "{{ item.regexp }}"
    line:                              "{{ item.line }}"
  loop:
    # Default values on install
    #     ResourceDisk.Format             = y
    #     ResourceDisk.Filesystem         = ext4
    #     ResourceDisk.MountPoint         = /mnt/resource
    #     ResourceDisk.EnableSwap         = n
    #     ResourceDisk.SwapSizeMB         = 0
    #     ResourceDisk.MountOptions       = None
    - { state: 'present',  regexp: 'ResourceDisk.Format=',               line: 'ResourceDisk.Format=y'                                      }
    - { state: 'present',  regexp: 'ResourceDisk.EnableSwap=',           line: 'ResourceDisk.EnableSwap=y'                                  }
    - { state: 'present',  regexp: 'ResourceDisk.SwapSizeMB=',           line: 'ResourceDisk.SwapSizeMB={{ swap_size_mb | default(2052) }}' }
    - { state: 'present',  regexp: 'ResourceDisk.MountPoint=',           line: 'ResourceDisk.MountPoint=/mnt'                               }
    - { state: 'present',  regexp: 'AutoUpdate.Enabled=',                line: 'AutoUpdate.Enabled=y'                                       }
    - { state: 'present',  regexp: 'AutoUpdate.UpdateToLatestVersion=',  line: 'AutoUpdate.UpdateToLatestVersion=y'                         }
    - { state: 'present',  regexp: 'Extensions.WaitForCloudInit=',       line: 'Extensions.WaitForCloudInit=y'                              }
  register:                            wagent_configuration
  vars:
    swap_size_mb:                      "{{ (sap_swap | selectattr('tier', 'search', node_tier) | list | first).swap_size_mb }}"
  when:
    - swap_size | int == 0

- name:                                "1.1 Swap - Reboot"
  when:
    - wagent_configuration.changed
  block:

    - name:                            "1.1 Swap - Reboot"
      become:                          true
      ansible.builtin.reboot:
        reboot_timeout:                300
        post_reboot_delay:             10
      failed_when:                     false

    - name:                            "1.1 Swap - Clear the failed state of hosts"
      ansible.builtin.meta:            clear_host_errors

  # Wait for Connection after reboot
    - name:                            "1.1 Swap - Wait for system to become reachable"
      ansible.builtin.wait_for_connection:
        delay:                         10
        timeout:                       300

...
