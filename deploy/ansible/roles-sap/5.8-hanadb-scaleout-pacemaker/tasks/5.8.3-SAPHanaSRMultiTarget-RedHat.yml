# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

---

# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  Implement the Python system replication hook SAPHanaSR-Scale-out                    |
# |  Ref: https://learn.microsoft.com/en-us/azure/sap/workloads/sap-hana-high-availability-scale-out-hsr-rhel?tabs=lb-portal#create-sap-hana-cluster-resources
# |  Begin: configuration for SAPHanaSR-Scale-out python hook                            |
# |                                                                            |
# +------------------------------------4--------------------------------------*/

- name:                                "5.8 HANA Pacemaker Scale-out - HANA 2.0 only - Implement the Python system replication hook SAPHanaSR-Scale-out MultiTarget"
  when:
                                       - node_tier == 'hana'
                                       - hdb_version[0:2] == "2."
                                       - database_high_availability
  block:
    - name:                            "5.8 HANA Pacemaker Scale-out - Generate list of deployed packages on current host"
      ansible.builtin.package_facts:

    # for RHEL, ensure resource-agents-sap-hana-Scale-out is installed

    - name:                            "5.8 HANA Pacemaker Scale-out - Ensure resource-agents-sap-hana is absent (REDHAT)"
      ansible.builtin.package:
        name:                          resource-agents-sap-hana
        state:                         absent
      when:
        - ansible_facts.packages['resource-agents-sap-hana'] is defined

    - name:                            "5.8 HANA Pacemaker Scale-out - Ensure resource-agents-sap-hana-scaleout is installed (REDHAT)"
      ansible.builtin.package:
        name:                          resource-agents-sap-hana-scaleout
        state:                         present
      when:
        - ansible_facts.packages['resource-agents-sap-hana-scaleout'] is not defined

    # <TODO> add package verification for RHEL based on link https://access.redhat.com/articles/3397471

    - name:                            "5.8 HANA Pacemaker Scale-out - Check HANA DB Version and register"
      become_user:                     "{{ db_sid | lower }}adm"
      become:                          true
      ansible.builtin.command:         /hana/shared/{{ db_sid | upper }}/HDB{{ db_instance_number }}/HDB version
      register:                        hdb_version_output
      changed_when:                    false

    - name:                            "5.8 HANA Pacemaker Scale-out - Extract SAP HANA version number"
      ansible.builtin.set_fact:
        hana_version_str: "{{ hdb_version_output.stdout | regex_search('version:\\s+([\\d\\.]+)', '\\1') | first }}"

    - name:                            "5.8 HANA Pacemaker Scale-out - Assert SAP HANA version is greater than SAP HANA 2.0 SP5"
      ansible.builtin.assert:
        that:
          - hana_version_str is version('2.00.050', '>=')
        fail_msg:                      "Installed HANA version is not greater than SAP HANA 2.0 SP5"
        success_msg:                   "Installed HANA version is greater than SAP HANA 2.0 SP5"
      register:                        hana_sp_version
      when: hdb_version_output.stdout is search("version")

    - name:                            "5.8 HANA Pacemaker Scale-out - Check if 'myHooks' file exists in /hana/shared directory"
      ansible.builtin.stat:
        path:                          /hana/shared/myHooks
      register:                        my_hooks_stat

    - name:                            "5.8.5 HANA Pacemaker Scale-out - Remove Global.ini flag, if cluster was not created by the deployment automation"
      when:                            not hana_cluster_existence_check
      ansible.builtin.file:
        path:                          /etc/sap_deployment_automation/sap_deployment_global_ini.txt
        state:                         absent


    - name:                            "5.8 HANA Pacemaker Scale-out - Configure global.ini for host name resolution if global.ini file does not exist"
      when:                            not hana_cluster_existence_check
      block:

        - name:                        "5.8 HANA Pacemaker Scale-out - Execute HANA StopSystem on both sites"
          when:                        ansible_hostname in [ primary_instance_name , secondary_instance_name ]
          become_user:                 "{{ db_sid | lower }}adm"
          become:                      true
          ansible.builtin.command:     "{{ sapcontrol_command }}  -function StopSystem  {{ db_sid | upper }}"
          failed_when:                 false
          changed_when:                false
          register:                    hana_system_stopped

        - name:                        "5.8 HANA Pacemaker Scale-out - Stop HANA on both nodes"
          when:
                                       - ansible_hostname in [ primary_instance_name , secondary_instance_name ]
          ansible.builtin.include_tasks:
            file:                      ../../../roles-misc/0.4-helpers/tasks/04.02-stop_hana.yml
            apply:
              environment:
                PATH:                  /usr/bin:/usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}:/usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe
                HOME:                  /usr/sap/{{ db_sid | upper }}/home
                DIR_LIBRARY:           /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe
                LD_LIBRARY_PATH:       /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe
                SAPSYSTEMNAME:         "{{ db_sid | upper }}"
          vars:
            allow_world_readable_tmpfiles: true

        - name:                        "5.8 HANA Pacemaker Scale-out - Copy Python hooks from /usr/share/SAPHanaSR-ScaleOut/ to /hana/shared/myHooks/ (RHEL)"
          ansible.builtin.copy:
            src:                       /usr/share/SAPHanaSR-ScaleOut/{{ item }}
            dest:                      /hana/shared/myHooks/
            remote_src:                true
            owner:                     root
            group:                     root      # TODO - check the correct group once the python hook package is installed
            mode:                      '0644'
          loop:
            - SAPHanaSR.py
            - ChkSrv.py
          when: not my_hooks_stat.stat.exists

        - name:                        "5.8 HANA Pacemaker Scale-out - Change ownership of the directory"
          become:                      true
          become_user:                 root
          ansible.builtin.file:
            path:                      /hana/shared/myHooks
            state:                     directory
            recurse:                   true
            owner:                     "{{ db_sid | lower }}adm"
            group:                     sapsys

        - name:                        "5.8 HANA Pacemaker Scale-out - Prepare global.ini for host name resolution"
          community.general.ini_file:
            path:                      "/hana/shared/{{ db_sid | upper }}/global/hdb/custom/config/global.ini"
            section:                   "system_replication_hostname_resolution"
            mode:                      0644
            state:                     present
            option:                    "{{ hostvars[item].ansible_host }}"
            value:                     "{{ hostvars[item].virtual_host }}"
          with_items:
            - "{{ groups[(sap_sid | upper)~'_DB' ] }}"

        - name:                        "5.8 HANA Pacemaker Scale-out - Adjust global.ini on each cluster node ( RHEL without susChkSrv/susTkOver )"
          when:
                                       - ansible_hostname == primary_instance_name or ansible_hostname == secondary_instance_name
          ansible.builtin.blockinfile:
            path:                      /hana/shared/{{ db_sid | upper }}/global/hdb/custom/config/global.ini
            block: |
                                       [ha_dr_provider_SAPHanaSR]
                                       provider = SAPHanaSR
                                       path = /hana/shared/myHooks
                                       execution_order = 1

                                       [ha_dr_provider_chksrv]
                                       provider = ChkSrv
                                       path = /hana/shared/myHooks
                                       execution_order = 2
                                       action_on_lost = kill

                                       [trace]
                                       ha_dr_saphanasr = info
                                       ha_dr_chksrv = info

        # Ref: https://learn.microsoft.com/en-us/azure/sap/workloads/sap-hana-high-availability-scale-out-hsr-rhel?tabs=lb-portal#create-sap-hana-cluster-resources
        - name:                        "5.8 HANA Pacemaker Scale-out - Create sudoers file for /etc/sudoers.d/20-saphana for RHEL"
          ansible.builtin.template:
            src:                       "20-saphana-rhel.j2"
            dest:                      "/etc/sudoers.d/20-saphana"
            mode:                      "0440"
            owner:                     root
            group:                     root
            # validate:                          /usr/sbin/visudo -cf %s

        - name:                        "5.8 HANA Pacemaker Scale-out - Execute HANA StartSystem on both sites"
          when:                        ansible_hostname in [ primary_instance_name , secondary_instance_name ]
          become_user:                 "{{ db_sid | lower }}adm"
          become:                      true
          ansible.builtin.command:     "{{ sapcontrol_command }} -function StartSystem"
          failed_when:                 false
          changed_when:                false
          register:                    hana_system_started

        - name:                        "5.8 HANA Pacemaker Scale-out - Start HANA on both nodes"
          when:
                                       - ansible_hostname in [ primary_instance_name , secondary_instance_name ]
          ansible.builtin.include_tasks:
            file:                        ../../../roles-misc/0.4-helpers/tasks/04.01-start_hana.yml
            apply:
              environment:
                PATH:                    /usr/bin:/usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}:/usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe
                HOME:                    /usr/sap/{{ db_sid | upper }}/home
                DIR_LIBRARY:             /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe
                LD_LIBRARY_PATH:         /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe
                SAPSYSTEMNAME:           "{{ db_sid | upper }}"
          vars:
            allow_world_readable_tmpfiles: true

# # REDHAT only
# # This needs to be run on all the nodes where HANA is deployed.
    - name:                            Verify that the hook script is working as expected (REDHAT)"
      when:
                                       - ansible_hostname == primary_instance_name
      block:
        - name:                        "5.8 HANA Pacemaker Scale-out - Verify the hook Installation (REDHAT)"
          become_user:                 "{{ db_sid | lower }}adm"
          become:                      true
          ansible.builtin.shell: |
                                       set -o pipefail
                                       awk '/ha_dr_SAPHanaSR.*crm_attribute/ \
                                       { printf "%s %s %s %s\n",$2,$3,$5,$16 }' nameserver_*
          args:
            chdir:                     /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/{{ hostvars[primary_instance_name].virtual_host }}/trace
          register:                    saphanasr
          until:                       saphanasr.stdout is search("SOK")
          retries:                     "{{ hsr_replication_retry_count }}"
          delay:                       "{{ hsr_replication_retry_delay }}"
      rescue:
        - name:                        "[Rescue] 5.8 HANA Pacemaker Scale-out - Reload HA Provides"
          become_user:                 "{{ db_sid | lower }}adm"
          become:                      true
          ansible.builtin.shell:       hdbnsutil -reloadHADRProviders
          register:                    saphanasr_reload
          failed_when:                 saphanasr_reload.rc != 0
          args:
            chdir:                     /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/exe

        - name:                        "[Rescue] 5.8 HANA Pacemaker Scale-out - Pause to give HANA replication time to stabilize"
          ansible.builtin.wait_for:
            timeout:                   "{{ rescue_hsr_status_report_wait_in_s }}"

        - name:                        "[Rescue] 5.8 HANA Pacemaker Scale-out - Verify the hook Installation (REDHAT)"
          become_user:                 "{{ db_sid | lower }}adm"
          become:                      true
          ansible.builtin.shell: |
                                       set -o pipefail
                                       awk '/ha_dr_SAPHanaSR.*crm_attribute/ \
                                       { printf "%s %s %s %s\n",$2,$3,$5,$16 }' nameserver_*
          args:
            chdir:                     /usr/sap/{{ db_sid | upper }}/HDB{{ db_instance_number }}/{{ hostvars[primary_instance_name].virtual_host }}/trace
          register:                    saphanasr
          until:                       saphanasr.stdout is search("SOK")
          retries:                     "{{ hsr_replication_retry_count }}"
          delay:                       "{{ hsr_replication_retry_delay }}"
      always:
        - name:                        "5.8 HANA Pacemaker Scale-out - Clear the failed state of hosts (REDHAT)"
          ansible.builtin.meta:        clear_host_errors

    - name:                            "5.8 HANA Pacemaker Scale-out - Verify the hook Installation"
      ansible.builtin.debug:
        var:                           saphanasr
        verbosity:                     2

    - name:                            "5.8 HANA Pacemaker Scale-out - Clear the failed state of hosts"
      ansible.builtin.meta:            clear_host_errors


# Note: We do not configure Hook on Majority maker, only installation is needed. Unfortunately since this task runs on HANA VM's only, Majority maker is skipped.
# Hook packages are deployed on Majority maker in task 1.18-scaleout-pacemaker
