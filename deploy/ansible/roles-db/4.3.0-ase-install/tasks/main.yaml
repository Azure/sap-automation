# /*---------------------------------------------------------------------------8
# |                                                                            |
# |                Perform the Sybase ASE Software installation                    |
# |                  SAP: Register BOM                                         |
# |                  create .params directory                                  |
# |                  Export environment variables for Sybase Installation      |
# |                  Run the Sybase ASE installer in silent mode.           |
# |                                                                            |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
---

- name: Check if reboot is needed (SUSE)
  include_tasks: check_reboot_is_needed_suse.yml
  when: ansible_os_family == 'Suse'

- name: Check if users were already created by a sap specific OS configuration
  include_tasks: check_sap_users.yml

- name: Prepare installation directories
  include_tasks: create_install_directories.yml
  when: sidadm_user_status is changed

- name: Create ini files for unattended installation
  include_tasks: create_ini_files.yml

- name: Install DB Instance
  include_tasks: install_db_instance.yml

- name: Check if installation finished or you need to reset
  include_tasks: check_db_installation.yml

- name: Remove parameter file
  include_tasks: cleanup_ini_file.yml

...
# /*---------------------------------------------------------------------------8
# |                                   END                                      |
# +------------------------------------4--------------------------------------*/