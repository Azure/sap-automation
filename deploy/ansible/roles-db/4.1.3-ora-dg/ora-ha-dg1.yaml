#PREREQUISITES
# -Prepare Source
#  -Add disk /oraBackup ---Don
#   -Configuration TNS.ora etc

# -Prepare Target
# - Configuration TNS.ora etc

# -Backup on Primary(Re-do logs)

# -Mount on Secondary
 #   Logic to move the Archive files to secondary

# -Restore on Secondary

# -DG Setup
#   -Broker configuration\\

#Configuring the Backup using BRBackupS




#Check Is the DB restore Complete 
#If "SecDBrestore completed"= true
#then skip

#else
  #DB restore -RMAN Recovery

# 2 Playbooks
#DG set up 
#DB restore -RMAN Recovery



    - block:
            - name:                    Restore STDBY_DB
              ansible.builtin.include_role:
                name:                  roles-db/4.1.1-restore-standby                         # Configures the Pacemaker cluster with Azure fence agent
              when:                    stdby_restore = 'true'
          tags:
            - 4.1.1-restore-standby

            - name:                        DG_Setup
              ansible.builtin.include_role:
              name:                      roles-db/4.1.2-ora-dgsetup