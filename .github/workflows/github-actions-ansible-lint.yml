
name: Ansible Lint
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Upgrade ansible.windows collection
        run: ansible-galaxy collection install ansible.windows -f
      - uses: ansible/ansible-lint-action@v6.8.2
